You are an expert at migrating a small multi-file Java project to idiomatic Go.
Translate the full project, preserving package boundaries with sensible Go package structure.
Do not add func main() unless the source requires an executable.

Required project contracts:
- `model.User` must remain a simple data type with readable `id`, `name`, and `email` state.
- `repository.UserRepository` must remain an interface with `save`, `findById`, and `findAll` behavior.
- `repository.InMemoryUserRepository` must store users in memory and return a copy-safe collection result for `findAll`.
- `service.UserValidator` must reject null users and blank names or emails.
- `service.UserService` must call validation before saving and must delegate persistence through the repository interface.
- `app.UserFacade` must expose create, get, and list flows by composing validator, repository, and service.

The migrated Go project must pass `go build ./...` and `go test ./...`.
