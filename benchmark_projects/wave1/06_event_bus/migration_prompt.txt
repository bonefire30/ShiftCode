You are an expert at migrating a small multi-file Java project to idiomatic Go.
Translate the full project, preserving package boundaries with sensible Go package structure.
Do not add func main() unless the source requires an executable.

Required project contracts:
- `event.EventBus` must allow listener registration and publish to all listeners.
- `event.EventListener` must stay as the callback contract.
- `event.UserCreatedEvent` must remain a concrete event carrying a user id.
- `app.UserRegistrationService` must publish `UserCreatedEvent` after registration.
- listener side effects must remain observable so tests can verify dispatch behavior.

The migrated Go project must pass `go build ./...` and `go test ./...`.
